apiVersion: v1
kind: Service
metadata:
  name: python-service
  labels:
    app: python-app
spec:
  type : LoadBalancer
  selector:
    app: python-app
  ports:
    - protocol: TCP
      port: 3030
      targetPort: 3030