apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    name: redis-service
    app: python-demo-app
spec:
  selector:
    name: redis-pod
    app: python-demo-app
  ports:
  - port: 9090
    targetPort: 9090